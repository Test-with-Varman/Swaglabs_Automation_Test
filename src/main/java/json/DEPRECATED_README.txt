DEPRECATED: This folder previously contained runtime JSON test data.

Canonical resource location is: src/main/resources/data.json
The code now loads JSON from the classpath. These files are kept for
backward compatibility. You may remove them if you no longer need legacy copies.
